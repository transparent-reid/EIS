# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/eis
    username: root
    password: muguohong666
  mvc:
    hiddenmethod:
      filter:
        enabled: true